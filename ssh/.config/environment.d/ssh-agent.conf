# Point SSH at the agent socket opened by ssh-agent.service (systemd --user).
# Read by the systemd user session at login, so terminals and GUI apps under
# sway all inherit it. %t in the unit == $XDG_RUNTIME_DIR.
SSH_AUTH_SOCK=${XDG_RUNTIME_DIR}/ssh-agent.socket
